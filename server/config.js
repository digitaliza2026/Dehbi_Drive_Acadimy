export const config = {
  port: 3001,
  jwtSecret: 'dehbi-drive-academy-secret-key-change-in-production',
  admin: {
    username: 'admin',
    // Default password: admin123 (change after first login)
    password: 'admin123'
  }
};
